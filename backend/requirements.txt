# =====================================================================
# Skill 制造工厂后端依赖清单
# 框架：Flask + SQLAlchemy + Celery + Redis
# =====================================================================

# Web 框架
Flask==3.1.0
Flask-RESTful==0.3.10
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-CORS==5.0.1

# 数据库驱动
psycopg2-binary==2.9.10
SQLAlchemy==2.0.40

# 任务队列
celery==5.5.2
redis==5.2.1
flower==2.0.1

# AI 模型接入（兼容 OpenAI 风格接口）
openai==1.77.0

# 配置与环境变量
python-dotenv==1.1.0

# 数据验证
marshmallow==3.26.1
pydantic==2.11.3

# 加密（API Key 加密存储）
cryptography==46.0.5

# YAML 解析（用于 SKILL.md frontmatter）
PyYAML==6.0.2

# 工具库
python-slugify==8.0.4
structlog==25.3.0
tenacity==9.1.2

# 测试
pytest==8.3.5
pytest-flask==1.3.0

# Gunicorn（生产环境 WSGI 服务器）
gunicorn==23.0.0
